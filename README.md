# shopthetop247 — Next.js Affiliate Site

This is a starter Next.js (App Router) project for shopthetop247.

## Quick start

1. Install dependencies: `npm install`
2. Run dev server: `npm run dev`

## Notes
- Tailwind is included; run `npm run dev` after installing deps.
- Replace placeholder images in `/public/images` with your real assets.
- Update affiliate links in `lib/products.js`.
